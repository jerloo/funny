token = ''
baseUrl = 'http://localhost:5000/'
debug = false

// 获取所有站点列表(用作测试)
getPosts() {
  return httpreq('GET', baseUrl + 'api/site/list', {

  }, {
    Authorization = 'Bearer ' + token
  }, debug)
}