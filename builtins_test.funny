
// Echo something
echo('arg1', 2, ['3',4])

// Echo something with newline
echoln('arg1', 2, ['3',4])

// Get now time
now()

// Base64 encode
b64en('data')

// Base64 decode
b64de('data')

// Assert
assert(1==1)

// Get length of something
len('length')

// Get md5 of content
md5('content')

// Return max of between value
max(a, b)

// Return min of between value
min(a, b)

// Typeof something
// typeof([1,2])

// Create new uuid string
uuid()

// Http request
httpreq('GET', 'https://baidu.com', {}, {}, true)