// Echo something
echo('arg1', 2, '3', 4)

// Echo something with newline
echoln('arg1', 2, '3', 4)

// Get now time
now()

// Base64 encode
b64en('data')

// Base64 decode
b64de('data')

// Assert
assert(2 > 1)

// Get length of something
len('length')

// Get md5 of content
md5('content')

// Return max of between value
max(1, 2)

// Return min of between value
min(1, 2)

// Typeof something
typeof(1, 2)

// Create new uuid string
uuid()

// Http request
httpreq('GET', 'https://baidu.com', {

}, {

}, true)

// test func
// new line
testFunc() {

}

httpreq('GET', 'https://baidu.com', {

}, {

}, true)